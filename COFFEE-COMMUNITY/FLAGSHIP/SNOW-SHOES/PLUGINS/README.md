# Snow Shoes — Coffee plugin pages (α)

These HTML files are **Coffee stack** builds meant to load inside **Snow Shoes α** as **iframe** plugins (see `snow-shoes-plugins.json`).

| File | Replaces (reference POC) |
|------|---------------------------|
| **`COFFEE-SEQUENCER-ALPHA.html`** | `../TEST/SNOW-SHOES4-SEQUENCER.html` (Tailwind + standalone Web Audio) |
| **`COFFEE-SUBZERO-MPC-ALPHA.html`** | `../TEST/SNOW-SHOES5-SUBZERO.html` (same) |

**Reference / demos (unchanged):** keep using **`../TEST/`** Tailwind POCs for layout and behavior reference while iterating on these α pages.

Scripts are loaded with paths relative to **`PLUGINS/`** → `../../../../` = `COFFEE-SOURCE/`.
